# Cart Review Demo

This is a small Python project used for the software testing course code review experiment.

The project models a shopping cart and provides calculation utilities. The experiment uses a feature branch to add discount calculation, then performs review, rejection, revision, approval, and merge.

## Run tests

```powershell
python -m unittest discover -s tests
```

